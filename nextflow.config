plugins {
    id 'nf-amazon'
}

profiles {
    standard {
        docker.enabled = true
        docker.runOptions = '-u \$(id -u):\$(id -g)'
        
        // This instructs Nextflow to use your EC2's IAM Instance Profile for S3 access
        aws.client.anonymous = false
    }
}
